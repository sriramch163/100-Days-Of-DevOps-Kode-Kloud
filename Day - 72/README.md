# Day 72 - Jenkins Parameterized Builds

## 🎯 Objective

The objective of this task is to understand and validate the basic functionality of parameterized builds in Jenkins. By completing this task, the user will gain hands-on experience in:

- Creating a parameterized Jenkins job
- Defining different types of build parameters (String and Choice)
- Passing parameters to a build
- Executing shell commands using parameter values
- Verifying successful execution using user-defined inputs

This task ensures familiarity with Jenkins UI navigation and fundamental CI job configuration concepts.

## 📁 Directory Structure
```
Day - 72/
├── Task/
│   └── task.png                    # Daily challenge requirements
├── Steps/
│   ├── adding para.png             # Adding parameters configuration
│   ├── build with para.png         # Build execution with parameters
│   ├── console output.png          # Build console output verification
│   ├── create job.png              # Jenkins job creation process
│   ├── creating choice para.png    # Choice parameter configuration
│   ├── execute shell build step.png # Shell execution setup
│   ├── login.png                   # Jenkins login process
│   └── paramaterized.png           # Parameterized build configuration
└── Steps.docx                     # Detailed step documentation
```

## 🛠️ Key Components
- **String Parameters**: Dynamic text input for builds
- **Choice Parameters**: Predefined options for build execution
- **Shell Command Integration**: Using parameters in build scripts
- **Build Verification**: Console output validation

## ✅ Solution Highlights
- Configured parameterized Jenkins job with multiple parameter types
- Implemented shell commands using parameter variables
- Validated successful parameter passing and execution
- Documented complete workflow with UI screenshots

## 📝 Learning Outcomes
- Jenkins parameterized build configuration
- Parameter types and usage patterns
- Build script integration with parameters
- CI/CD job customization fundamentals